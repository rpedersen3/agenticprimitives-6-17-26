# @agenticprimitives/browser-identity

## 1.0.0-alpha.6

### Initial (spec 264 Phase 0)

- The browser-integration adapter seam: `fedcmAvailable()` + `chooseSignIn()` + the injection types.
- FedCM-first, not FedCM-only (ADR-0031). `private: true` until the FedCM RP path lands (Phase 1).
