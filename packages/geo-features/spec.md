# Spec pointer

Canonical spec: [`../../specs/251-skills-and-geo-features.md`](../../specs/251-skills-and-geo-features.md)

The off-chain geo-CLAIM layer of the generic skills/geo substrate. Features on chain
(`GeoFeatureRegistry`); claims are private vault credentials pointing to a `(featureId, version)`.
