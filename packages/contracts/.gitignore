lib/
out/
cache/
broadcast/
.env
# R7.3 — deployments-base-sepolia.json IS committed (public on-chain
# addresses; downstream consumers depend on it via
# `@agenticprimitives/contracts/deployments/base-sepolia`). The local
# anvil JSON is still ignored — its addresses are developer-machine-
# specific (depend on devnet block/timestamp at deploy).
deployments-anvil.json
lcov.info
coverage-r6-9.json
# R9.4 / R9.5 — Echidna + Medusa write coverage + replay corpus +
# crytic-export here. Both corpora ARE audit-evidence (sequences that
# exercised previously-unhit branches), but generated content +
# reproducible from harness + seed. Nightly + weekly workflows
# artifact-upload these dirs so they're preserved out-of-tree.
echidna-corpus/
medusa-corpus/
crytic-export/
