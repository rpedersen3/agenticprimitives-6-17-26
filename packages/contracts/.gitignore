lib/
out/
cache/
broadcast/
.env
# R7.3 — deployments-base-sepolia.json IS committed (public on-chain
# addresses; downstream consumers depend on it via
# `@agenticprimitives/contracts/deployments/base-sepolia`). The local
# anvil JSON is still ignored — its addresses are developer-machine-
# specific (depend on devnet block/timestamp at deploy).
deployments-anvil.json
lcov.info
coverage-r6-9.json
# R9.4 — Echidna writes coverage + replay corpus + crytic-export here.
# The corpus IS audit-evidence (sequences that exercised previously-
# unhit branches), but it's generated content + reproducible from the
# harness + a seed. The nightly workflow artifact-uploads the corpus
# dir so it's still preserved out-of-tree.
echidna-corpus/
crytic-export/
