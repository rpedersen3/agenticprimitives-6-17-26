# Spec pointer

Canonical spec: [`../../specs/251-skills-and-geo-features.md`](../../specs/251-skills-and-geo-features.md)

The off-chain skill-CLAIM layer of the generic skills/geo substrate. Definitions on chain
(`SkillDefinitionRegistry`); claims are private vault credentials pointing to a `(skillId, version)`.
