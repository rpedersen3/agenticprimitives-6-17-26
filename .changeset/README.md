### Initial bump

The first `0.1.0-alpha.1` bump (the "H7 close" baseline) was done by writing the version manually to every `package.json` (see commit `H7-E.3`). Subsequent bumps go through the standard changeset flow.
